# Binaries
bin/
luminarr
radarr-import
*.exe
*.exe~
*.dll
*.so
*.dylib

# Build artifacts
dist/

# Test artifacts
*.test
*.out
coverage.txt

# Go workspace
go.work
go.work.sum

# Config with real secrets — use config.example.yaml as template
config.yaml
*.db
*.db-shm
*.db-wal

# OS
.DS_Store
Thumbs.db

# Claude Code session data
.claude/
!.claude/settings.json

# Editor
.idea/
.vscode/
*.swp
*.swo
*~

# Node
web/ui/node_modules/
web/ui/.vite/

# Built frontend — included in repo so `go build` works without npm
# Uncomment the line below to exclude it (CI should run npm run build first)
# web/static/

# Local dev scripts
rebuild-restart.sh

# Air (hot reload)
tmp/

# Luminarr runtime data
luminarr.db
luminarr.db-shm
luminarr.db-wal
